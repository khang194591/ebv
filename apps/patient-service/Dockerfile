# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build patient-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t patient-service`.
FROM docker.io/node:lts-alpine

ARG PATIENT_HOST

ENV HOST=0.0.0.0
ENV PORT=${PATIENT_HOST}

WORKDIR /app

RUN addgroup --system patient-service && \
    adduser --system -G patient-service patient-service

COPY dist/apps/patient-service patient-service/
RUN chown -R patient-service:patient-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix patient-service --omit=dev -f install

CMD [ "node", "patient-service" ]
